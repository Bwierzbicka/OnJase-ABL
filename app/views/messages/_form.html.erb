<%= simple_form_for [@chat, @message] do |f| %>
  <%= f.input :content, placeholder: "Ask anything", required: true, label: false %>
  <%= f.button :submit, "Submit", class: "btn btn-primary float-end" %>
<% end %>
