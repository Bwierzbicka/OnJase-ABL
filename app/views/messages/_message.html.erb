<% if message.role == "user" %>
  <div class="d-flex justify-content-end mt-2 me-3">
     <div class="bg-light-blue user-message">
      <%= raw(render_markdown(message.content)) %>
      <%#= message.content %>
    </div>
  </div>
<% elsif message.role == "assistant" %>
  <div class="d-flex justify-content-start mb-3" id="<%= dom_id(message) %>">
    <div class="px-3 pb-0 pt-3">
      <%= message.content.present? ? raw(render_markdown(message.content)) : "..." %>
    </div>
  </div>
<% end %>
